const Middleware = require('./Middleware');
const EventCollector = require('./EventCollector');
const FrontendCollector = require('./FrontendCollector');

module.exports = {
  Middleware,
  EventCollector,
  FrontendCollector
};